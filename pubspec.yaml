name: tonote
description: Note on Web
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^9.0.0-nullsafety

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
